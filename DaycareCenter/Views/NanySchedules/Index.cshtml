@model IEnumerable<DaycareCenter.Models.NanySchedule>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Schedule_Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Shift_Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nany_Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Child_Id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Schedule_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Shift_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nany_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Child_Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Schedule_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Schedule_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Schedule_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
